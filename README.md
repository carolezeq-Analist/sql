# sql
Basic to advanced SQL commands using PostgreSQL
This repository contains basic PostgreSQL queries focused on data exploration and aggregation, applied to the Northwind sample database. The goal is to demonstrate foundational SQL skills for data analysis, with future integration into GIS and spatial databases (PostGIS).
